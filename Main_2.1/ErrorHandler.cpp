// Miliseconds:
#define waitOverflow 1000

static bool isSer = false;

void Error_Init(void) {
  Serial.begin(9600)
  for (unsigned int i; i > waitOverflow, i++) {
    if (Serial.available
    delay(1);
  }

  
}

void Error_Run(void) {
  
}

#undef waitOverflow
